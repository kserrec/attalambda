# AttaLambda 0.8.0 — unpublished candidate

The clean Linux archive from `3ae392629ecfb380d3dd31451c33fd5617d53f51` passes
isolated consumption and terminal checks before and after relocation. Final
current-head CI and delivery reconciliation are recorded in
[HANDOFF.md](../../HANDOFF.md); [PLAN.md](../../PLAN.md) retains phase evidence.
This candidate is not merged, tagged or published. Public downloads remain 0.7.0.

## Implemented source changes

- `(read-line UNIT)` supplies byte-preserving runtime input through the existing
  host, with lazy sharing, `OK(SOME(String))`, and `OK(NONE)` at EOF.
- No-file startup provides the `atta>` shell on a terminal. Explicit `--repl`
  permits redirected transcripts; `--no-history` disables persistent history.
- Six commands provide help, committed-name listing, standalone file loading,
  echo control, reset, and quit. Successful definitions remain lazy; earlier
  closures keep their captured bindings across redefinitions.
- The terminal editor handles multiline entries, editing, cancellation, input
  handoff, source-history recall, and completion from public and committed names. It runs no personal initialization files.
- Automatic echo uses the existing pure renderer. Use `:echo off` for raw
  functions: their rendering behavior remains unspecified.

The [API guide](../API.md#interactive-shell-unreleased) gives exact source,
input, history, and exit-status behavior. Existing files keep their
`#lang attalambda` declaration and execute without automatic expression echo.
Rat representations, unary-lambda purity, the public names, recursion rules,
and the old file/TCP/printing operations are preserved. No input-only release,
new evaluator, optimizer, package manager, or additional public binary platform
is introduced.

## Verification status

The complete corrected Racket CS 9.3 source suite passes 69 files / 26,845
assertions, 43 shared terminal methods, 6 visual terminal methods, 40-module
expanded purity and the full boundary inventory. Independent source, terminal,
and dependency-preparation reviews closed their confirmed findings. The pinned
Racket promise correction also passes 261 upstream tests.

The exact standalone archive contains 11 files and no Python. Its isolated
consumer uses standard-library Python test tooling, with no external Racket or
source checkout. All 25 CLI/transcript methods pass at the first path in 67.185s
and after relocation in 78.388s. The archive also passes runtime input, snapshot
transcripts, guide examples, provenance, dependency and cleanup checks.

| Candidate evidence | Value |
| --- | --- |
| Build revision | `3ae392629ecfb380d3dd31451c33fd5617d53f51` — clean tree |
| Archive | `attalambda-0.8.0-linux-x86_64.tar.gz`, 19,602,521 bytes |
| Archive SHA-256 | `1b4b4a338028b11fafe22a4b52c7862c77f9c57e0241e4e037c6b022f6761785` |
| Internal manifest SHA-256 | `e0597d0506f08ad0661c0dc9d1dab2ac1257afb8d799d3aca3cbb2725a44a95b` |
| SHA256SUMS SHA-256 | `a5382b07aa7190196f114536cf47e86d029a8017b43cc4cd934a88db25fc11ba` |

Later documentation-only evidence commits do not change the archive's build
revision. The milestone PR remains open; merge, tagging and publication are not
authorized. Exact commands, runtime/image pins, review coverage, CI state,
consumer logs and cleanup receipts are in the handoff.
